# 协调器模式

<cite>
**本文档引用的文件**
- [orchestrator.ts](file://packages/core/src/orchestrator/orchestrator.ts)
- [scheduler.ts](file://packages/core/src/orchestrator/scheduler.ts)
- [queue.ts](file://packages/core/src/task/queue.ts)
- [task.ts](file://packages/core/src/task/task.ts)
- [consensus.md](file://docs/consensus.md)
- [model-routing.md](file://docs/model-routing.md)
- [consensus.ts](file://packages/core/examples/patterns/consensus.ts)
- [multi-model-team.ts](file://packages/core/examples/basics/multi-model-team.ts)
- [team-collaboration.ts](file://packages/core/examples/basics/team-collaboration.ts)
- [orchestrator.test.ts](file://packages/core/tests/orchestrator.test.ts)
- [consensus.test.ts](file://packages/core/tests/consensus.test.ts)
- [short-circuit.test.ts](file://packages/core/tests/short-circuit.test.ts)
- [types.ts](file://packages/core/src/types.ts)
</cite>

## 更新摘要
**所做更改**
- 新增共识验证功能章节，详细介绍 runConsensus 和 per-task verify 钩子
- 新增模型路由功能章节，说明 ModelRoutingPolicy 的工作原理
- 更新架构概览图以包含新的共识验证和模型路由组件
- 增强了协调器模式的高级协作能力描述
- 添加了实际代码示例和配置选项说明

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [共识验证系统](#共识验证系统)
7. [模型路由功能](#模型路由功能)
8. [依赖关系分析](#依赖关系分析)
9. [性能考虑](#性能考虑)
10. [故障排除指南](#故障排除指南)
11. [结论](#结论)

## 简介

协调器模式是 OpenMultiAgent 框架的核心设计模式，它通过一个临时的"协调器代理"将高层次的目标分解为可执行的任务。这个模式的关键价值在于：

- **自动任务分解**：协调器代理能够理解复杂的多步骤目标，并将其分解为具体的、可执行的任务
- **智能任务分配**：根据任务特性和代理能力进行最优分配
- **并行执行优化**：识别独立任务并行执行，提高整体效率
- **依赖管理**：自动处理任务间的依赖关系，确保正确的执行顺序
- **质量保证**：集成共识验证机制，确保任务结果的准确性和可靠性
- **成本控制**：通过模型路由功能实现智能的成本优化

协调器模式的核心思想是"先分解，后执行"——协调器负责规划和分解，而具体的工作由专门的代理完成。新增的共识验证和模型路由功能进一步增强了该模式的高级协作能力和成本效益。

## 项目结构

OpenMultiAgent 的协调器模式实现主要分布在以下模块中：

```mermaid
graph TB
subgraph "协调器核心"
OM[OpenMultiAgent<br/>主协调器类]
CO[协调器代理<br/>临时代理]
PQ[任务队列<br/>TaskQueue]
SCH[调度器<br/>Scheduler]
end
subgraph "代理系统"
AG[Agent<br/>基础代理]
AP[AgentPool<br/>代理池]
TM[Team<br/>团队管理]
end
subgraph "工具系统"
TR[ToolRegistry<br/>工具注册表]
TE[ToolExecutor<br/>工具执行器]
end
subgraph "高级功能"
CN[共识验证<br/>Consensus]
MR[模型路由<br/>ModelRouting]
end
OM --> CO
OM --> PQ
OM --> SCH
OM --> AP
OM --> TM
CO --> AG
AP --> AG
AG --> TR
TR --> TE
CN --> OM
MR --> OM
```

**图表来源**
- [orchestrator.ts:1040-1374](file://packages/core/src/orchestrator/orchestrator.ts#L1040-L1374)
- [scheduler.ts:96-322](file://packages/core/src/orchestrator/scheduler.ts#L96-L322)
- [consensus.md:1-64](file://docs/consensus.md#L1-L64)
- [model-routing.md:1-76](file://docs/model-routing.md#L1-L76)

**章节来源**
- [orchestrator.ts:1-800](file://packages/core/src/orchestrator/orchestrator.ts#L1-L800)
- [scheduler.ts:1-322](file://packages/core/src/orchestrator/scheduler.ts#L1-L322)

## 核心组件

### OpenMultiAgent 主协调器

OpenMultiAgent 是协调器模式的主要实现者，它提供了完整的多代理协调功能：

- **runTeam 方法**：执行团队级的自动协调运行
- **runTasks 方法**：执行显式提供的任务列表
- **createTeam 方法**：创建和管理团队
- **runConsensus 方法**：执行共识验证循环
- **状态管理**：跟踪团队状态和完成的任务数量

### 协调器代理（临时）

协调器代理是一个特殊的临时代理，具有以下特性：

- **一次性使用**：仅在分解阶段使用，不参与后续执行
- **专门职责**：负责将高层次目标分解为具体任务
- **JSON 输出格式**：严格遵循预定义的 JSON 结构
- **合成能力**：在所有任务完成后合成最终结果
- **验证集成**：支持在任务规范中嵌入验证配置

### 任务队列系统

任务队列负责管理任务的生命周期和依赖关系：

- **依赖解析**：自动解析任务间的依赖关系
- **状态管理**：跟踪任务的 pending、blocked、in_progress、completed、failed 状态
- **事件驱动**：通过事件系统通知状态变化
- **拓扑排序**：确保任务按正确的顺序执行
- **验证钩子**：支持 per-task 验证功能

### 调度器系统

调度器提供多种任务分配策略：

- **round-robin**：轮询分配，确保公平性
- **least-busy**：优先分配给最空闲的代理
- **capability-match**：基于能力匹配的智能分配
- **dependency-first**：优先处理关键路径上的任务

**章节来源**
- [orchestrator.ts:900-1785](file://packages/core/src/orchestrator/orchestrator.ts#L900-L1785)
- [scheduler.ts:96-322](file://packages/core/src/orchestrator/scheduler.ts#L96-L322)

## 架构概览

协调器模式的整体架构如下，现已集成了共识验证和模型路由功能：

```mermaid
sequenceDiagram
participant Client as 客户端
participant OM as OpenMultiAgent
participant CO as 协调器代理
participant PQ as 任务队列
participant SCH as 调度器
participant AP as 代理池
participant AG as 工作代理
participant CN as 共识验证
participant MR as 模型路由
Client->>OM : runTeam(team, goal, options)
OM->>OM : 检查是否为简单目标
alt 简单目标
OM->>MR : 应用模型路由规则
MR-->>OM : 返回路由后的配置
OM->>AG : 直接分配给最佳代理
AG-->>OM : 执行结果
else 复杂目标
OM->>CO : 创建临时协调器代理
CO->>CO : 分解目标为任务
CO-->>OM : 返回任务数组(JSON)
OM->>PQ : 加载任务到队列
OM->>SCH : 自动分配任务
OM->>AP : 构建代理池
loop 任务执行循环
AP->>MR : 应用模型路由规则
MR-->>AP : 返回路由后的配置
AP->>AG : 分配任务给代理
AG-->>PQ : 任务完成
PQ-->>SCH : 更新状态
SCH-->>AP : 下一批任务
PQ->>CN : 触发验证钩子
CN->>CN : 执行共识验证
CN-->>PQ : 返回验证结果
end
OM->>CO : 合成最终结果
CO-->>OM : 最终答案
end
OM-->>Client : 团队运行结果
```

**图表来源**
- [orchestrator.ts:1061-1374](file://packages/core/src/orchestrator/orchestrator.ts#L1061-L1374)
- [orchestrator.ts:561-818](file://packages/core/src/orchestrator/orchestrator.ts#L561-L818)
- [consensus.ts:89-138](file://packages/core/examples/patterns/consensus.ts#L89-L138)

## 详细组件分析

### 目标复杂度分析与简单目标判断

协调器模式的核心在于能够区分简单目标和复杂目标：

#### 简单目标判断逻辑

```mermaid
flowchart TD
Start([开始分析目标]) --> CheckLength["检查目标长度<br/>字符数 ≤ 200"]
CheckLength --> LengthOk{"长度是否≤200?"}
LengthOk --> |否| Complex["标记为复杂目标"]
LengthOk --> |是| CheckPatterns["检查复杂度模式"]
CheckPatterns --> MatchPattern{"是否匹配任何复杂度模式?"}
MatchPattern --> |是| Complex
MatchPattern --> |否| Simple["标记为简单目标"]
Complex --> End([结束])
Simple --> End
```

**图表来源**
- [orchestrator.ts:147-150](file://packages/core/src/orchestrator/orchestrator.ts#L147-L150)
- [orchestrator.ts:96-121](file://packages/core/src/orchestrator/orchestrator.ts#L96-L121)

#### 复杂度模式识别

系统使用正则表达式识别以下复杂度信号：

1. **明确的序列化指令**：
   - "first ... then" 结构
   - "step N" 和 "phase N" 标记
   - 编号列表格式

2. **协作语言**：
   - "collaborate with/on/for" 指令
   - "coordinate the team/workers"
   - "review each other"
   - "work together"

3. **并行执行信号**：
   - "in parallel"
   - "concurrently"
   - "at the same time"

4. **多阶段产出**：
   - 包含多个动作动词的复合句
   - 使用连接词的多任务描述

#### 最佳代理选择算法

对于简单目标，系统使用关键词相似度算法选择最佳代理：

```mermaid
flowchart TD
Goal[输入目标] --> ExtractGoal["提取目标关键词"]
ExtractGoal --> LoopAgents["遍历所有代理"]
LoopAgents --> CalcScore["计算双向关键词相似度分数"]
CalcScore --> Compare["比较分数"]
Compare --> Better{"是否为当前最佳?"}
Better --> |是| UpdateBest["更新最佳代理"]
Better --> |否| NextAgent["检查下一个代理"]
UpdateBest --> NextAgent
NextAgent --> Done{"所有代理都检查完毕?"}
Done --> |否| CalcScore
Done --> |是| SelectAgent["选择最佳代理"]
SelectAgent --> End([结束])
```

**图表来源**
- [orchestrator.ts:170-194](file://packages/core/src/orchestrator/orchestrator.ts#L170-L194)
- [orchestrator.ts:173-185](file://packages/core/src/orchestrator/orchestrator.ts#L173-L185)

**章节来源**
- [orchestrator.ts:147-194](file://packages/core/src/orchestrator/orchestrator.ts#L147-L194)
- [orchestrator.ts:96-121](file://packages/core/src/orchestrator/orchestrator.ts#L96-L121)

### 临时协调器代理工作机制

临时协调器代理是协调器模式的关键组件，具有以下工作机制：

#### 协调器系统提示构建

协调器代理接收精心构建的系统提示，包含：

1. **团队名单**：列出所有可用代理及其能力和模型
2. **输出格式要求**：严格的 JSON 数组格式规范
3. **依赖指导原则**：如何正确设置任务依赖关系
4. **合成指令**：如何整合各个任务的结果
5. **验证配置**：支持在任务规范中嵌入验证选项

#### 任务规范解析

协调器代理输出的 JSON 格式必须包含：

```json
{
  "title": "任务标题",
  "description": "任务详细描述",
  "assignee": "代理名称",
  "dependsOn": ["依赖任务标题或ID"],
  "verify": {
    "mode": "refute",
    "quorum": 2,
    "maxRounds": 2,
    "onDissent": "revise"
  }
}
```

系统提供两种解析策略：

1. **代码围栏解析**：从 ` ```json ... ``` ` 代码围栏中提取 JSON
2. **数组边界解析**：从任意位置提取第一个 `[` 到最后一个 `]` 的内容

#### 降级处理机制

当协调器无法产生结构化输出时，系统会自动降级：

- 为每个代理创建一个任务
- 将原始目标作为任务描述
- 继续正常的任务执行流程

**章节来源**
- [orchestrator.ts:1554-1575](file://packages/core/src/orchestrator/orchestrator.ts#L1554-L1575)
- [orchestrator.ts:355-397](file://packages/core/src/orchestrator/orchestrator.ts#L355-L397)
- [orchestrator.ts:1229-1240](file://packages/core/src/orchestrator/orchestrator.ts#L1229-L1240)

### 任务解析与依赖分析

#### 任务加载流程

```mermaid
flowchart TD
Start([开始加载任务]) --> CreateTasks["创建任务实例<br/>生成唯一ID"]
CreateTasks --> MapTitles["建立标题→ID映射"]
MapTitles --> ResolveDeps["解析依赖关系"]
ResolveDeps --> CheckRefs{"依赖引用有效?"}
CheckRefs --> |是| AddTask["添加到队列"]
CheckRefs --> |否| SkipTask["跳过无效依赖"]
AddTask --> NextTask{"还有任务?"}
SkipTask --> NextTask
NextTask --> |是| ResolveDeps
NextTask --> |否| Complete([完成])
```

**图表来源**
- [orchestrator.ts:1651-1710](file://packages/core/src/orchestrator/orchestrator.ts#L1651-L1710)
- [orchestrator.ts:1664-1709](file://packages/core/src/orchestrator/orchestrator.ts#L1664-L1709)

#### 依赖图验证

系统提供完整的依赖图验证机制：

- **未知引用检查**：确保所有依赖任务都存在
- **循环检测**：防止任务间的循环依赖
- **自依赖检查**：避免任务依赖自身

#### 拓扑排序算法

使用 Kahn 算法进行拓扑排序，确保任务执行的正确顺序：

1. 计算每个任务的入度（依赖数量）
2. 将所有入度为 0 的任务加入队列
3. 重复处理队列中的任务，减少其后继任务的入度
4. 入度为 0 的任务加入结果序列

**章节来源**
- [orchestrator.ts:1651-1710](file://packages/core/src/orchestrator/orchestrator.ts#L1651-L1710)
- [task.ts:117-162](file://packages/core/src/task/task.ts#L117-L162)
- [task.ts:186-200](file://packages/core/src/task/task.ts#L186-L200)

### 调度策略详解

#### round-robin 轮询策略

最简单的分配策略，确保每个代理都能获得任务：

- 维护一个全局游标
- 按顺序分配给不同代理
- 避免某些代理过载

#### least-busy 最少忙碌策略

基于代理当前负载的智能分配：

- 统计每个代理的进行中任务数量
- 优先分配给负载最少的代理
- 在同一轮次内避免重复分配给同一代理

#### capability-match 能力匹配策略

基于关键词相似度的智能分配：

```mermaid
flowchart TD
Task[任务] --> ExtractTask["提取任务关键词"]
ExtractTask --> LoopAgents["遍历代理"]
LoopAgents --> CalcAffinity["计算双向关键词相似度"]
CalcAffinity --> ScoreA["任务关键词→代理文本"]
CalcAffinity --> ScoreB["代理关键词→任务文本"]
ScoreA --> Combine["组合分数"]
ScoreB --> Combine
Combine --> Best{"是否为最佳匹配?"}
Best --> |是| UpdateBest["更新最佳代理"]
Best --> |否| NextAgent["检查下一个代理"]
UpdateBest --> NextAgent
NextAgent --> Done{"所有代理检查完毕?"}
Done --> |否| CalcAffinity
Done --> |是| Assign["分配给最佳代理"]
Assign --> End([结束])
```

**图表来源**
- [scheduler.ts:245-284](file://packages/core/src/orchestrator/scheduler.ts#L245-L284)
- [scheduler.ts:266-278](file://packages/core/src/orchestrator/scheduler.ts#L266-L278)

#### dependency-first 关键路径策略

优先处理对其他任务影响最大的任务：

- 计算每个任务的"下游影响"（被多少任务等待）
- 按影响程度降序排列
- 在同一影响层级内使用轮询策略

**章节来源**
- [scheduler.ts:96-322](file://packages/core/src/orchestrator/scheduler.ts#L96-L322)

### 执行流程控制

#### 并发执行管理

系统支持最大并发度控制：

- **默认并发度**：5 个代理同时执行
- **代理池管理**：动态管理代理的生命周期
- **资源限制**：防止过度并发导致的资源耗尽

#### 错误处理与重试机制

```mermaid
flowchart TD
Start([任务开始]) --> Execute["执行任务"]
Execute --> Success{"执行成功?"}
Success --> |是| Complete["标记完成"]
Success --> |否| CheckRetries{"还有重试机会?"}
CheckRetries --> |是| Delay["指数退避延迟"]
CheckRetries --> |否| Fail["标记失败"]
Delay --> Execute
Complete --> End([结束])
Fail --> End
```

**图表来源**
- [orchestrator.ts:266-333](file://packages/core/src/orchestrator/orchestrator.ts#L266-L333)
- [orchestrator.ts:247-253](file://packages/core/src/orchestrator/orchestrator.ts#L247-L253)

#### 预算控制与成本管理

系统提供多层次的成本控制：

- **令牌预算**：限制总令牌使用量
- **代理预算**：限制单个代理的令牌使用
- **超预算处理**：优雅地停止进一步执行
- **共识预算**：共识验证也计入总预算

**章节来源**
- [orchestrator.ts:561-818](file://packages/core/src/orchestrator/orchestrator.ts#L561-L818)
- [orchestrator.ts:266-333](file://packages/core/src/orchestrator/orchestrator.ts#L266-L333)

## 共识验证系统

### runConsensus 核心功能

OpenMultiAgent 提供了强大的共识验证功能，通过 `runConsensus` 方法实现 proposer→judge 验证循环：

#### 基本用法示例

```typescript
const result = await orchestrator.runConsensus(team, 'Is this proof correct?', {
  proposer: { name: 'solver', model: 'claude-opus-4-6' },
  judges: [
    { name: 'judge-a', model: 'claude-opus-4-6' },
    { name: 'judge-b', model: 'claude-sonnet-4-6' },
  ],
  mode: 'refute',
  quorum: 2,
  maxRounds: 2,
  onDissent: 'revise',
})
```

#### 验证选项详解

| 选项 | 默认值 | 含义 |
|------|---------|------|
| `proposer` | — | 提出者代理，或 N-best 数组 |
| `judges` | — | 验证者名单，按顺序执行 |
| `mode` | `'refute'` | `'refute'`: 相同怀疑框架；`'lens'`: 不同角度 |
| `quorum` | `ceil(judges.length / 2)` | 达成共识所需的接受者数量 |
| `maxRounds` | `2` | 提出者↔验证者轮次上限 |
| `verdictSchema` | — | 可选的 Zod 模式验证 |
| `onDissent` | `'revise'` | `'revise'`: 反对时反馈给提出者；`'reject'`: 直接拒绝；`'keep'`: 保持答案 |

#### per-task 验证钩子

任务可以在 `runTasks` 流程中选择性地启用共识验证：

```typescript
await orchestrator.runTasks(team, [
  {
    title: 'derive-bound',
    description: 'Prove the O(n log n) bound.',
    assignee: 'mathematician',
    verify: { judges: [judgeA, judgeB], mode: 'refute', maxRounds: 2 },
  },
])
```

### 验证流程机制

```mermaid
flowchart TD
Start([开始验证]) --> Proposer["提出者生成答案"]
Proposer --> JudgeLoop["验证者循环"]
JudgeLoop --> Judge["验证者评估"]
Judge --> CheckQuorum{"达到共识?"}
CheckQuorum --> |是| Accept["接受答案"]
CheckQuorum --> |否| CheckDissent{"反对处理?"}
CheckDissent --> |revise| Revise["提出者修订"]
Revise --> JudgeLoop
CheckDissent --> |reject| Reject["拒绝答案"]
CheckDissent --> |keep| Accept
Accept --> End([结束])
Reject --> End
```

**图表来源**
- [consensus.ts:1330-1529](file://packages/core/src/orchestrator/orchestrator.ts#L1330-L1529)

### 预算管理和可观测性

共识验证完全集成到框架的预算控制系统中：

- **预算合并**：提出者和验证者的使用量都计入父预算
- **预算检查**：超出预算时停止进一步验证调用
- **追踪事件**：每个验证者的决定都通过 `onTrace` 发出 `consensus` 事件
- **共享内存**：反对意见写入共享内存（键：`consensus:round:N:dissent`）

**章节来源**
- [consensus.md:1-64](file://docs/consensus.md#L1-L64)
- [consensus.ts:89-138](file://packages/core/examples/patterns/consensus.ts#L89-L138)
- [consensus.test.ts:68-178](file://packages/core/tests/consensus.test.ts#L68-L178)

## 模型路由功能

### ModelRoutingPolicy 概述

模型路由功能允许用户以确定性的方式将不同的编排调用发送到不同的模型，而无需修改团队或代理配置：

#### 基本配置示例

```typescript
const modelRouting: ModelRoutingPolicy = {
  rules: [
    // 首席模型负责目标分解和最终合成
    { match: { phase: 'coordinator' }, route: { model: 'claude-opus-4-7' } },
    { match: { phase: 'synthesis' },   route: { model: 'claude-opus-4-7' } },
    // 叶子任务使用便宜的模型执行
    { match: { leaf: true }, route: { model: 'claude-haiku-4-5' } },
  ],
}

const result = await orchestrator.runTeam(team, goal, { modelRouting })
```

### 匹配维度详解

路由规则基于以下匹配维度：

| 字段 | 匹配条件 | 适用范围 |
|------|----------|----------|
| `phase` | 调用处于此编排阶段 | 每个调用 |
| `agent` | 运行代理名称等于此值 | 每个调用 |
| `taskRole` | 任务的 `role` 等于此值 | worker/delegated 调用 |
| `taskPriority` | 任务的 `priority` 等于此值 | worker/delegated 调用 |
| `leaf` | 任务在执行图中无后继任务 | worker/delegated 调用 |
| `hasDependencies` | 任务的 `dependsOn` 非空 | worker/delegated 调用 |

### 路由配置选项

当规则匹配时应用的覆盖配置：

| 字段 | 含义 |
|------|------|
| `model` | 匹配调用使用的模型（必需） |
| `provider` | 提供商覆盖（默认使用代理或默认提供商） |
| `baseURL` | OpenAI兼容或自托管端点的基础URL |
| `apiKey` | 匹配调用使用的API密钥 |
| `region` | Bedrock路由的AWS区域 |

### 调用路由范围

单个策略涵盖所有五个编排阶段：

- **`coordinator`** — `runTeam` 中的目标分解调用
- **`synthesis`** — `runTeam` 中的最终答案合成调用  
- **`worker`** — `runTeam` 和 `runTasks` 中的任务代理执行
- **`delegated`** — 通过 `delegate_to_agent` 到达的代理
- **`short-circuit`** — 当目标足够简单时跳过分解的单代理路径

### 安全保证

两个保证确保路由功能安全且可配置：

- **可选性**：省略 `modelRouting` 时，模型选择与之前完全相同
- **非突变性**：匹配的路由只构建该调用的临时有效配置，不会修改团队、代理配置或协调器默认值

### 成本分层示例

框架提供了成本分层管道的示例，展示了如何在相同的四阶段管道中运行两次（全部旗舰模型 vs. 分层混合），并打印每种模型的令牌和美元细分，让用户在采用前看到路由策略的节省效果。

**章节来源**
- [model-routing.md:1-76](file://docs/model-routing.md#L1-L76)
- [multi-model-team.ts](file://packages/core/examples/basics/multi-model-team.ts)

## 依赖关系分析

协调器模式涉及多个模块之间的复杂依赖关系，现已扩展包含新的共识验证和模型路由功能：

```mermaid
graph TB
subgraph "协调器层"
OM[OpenMultiAgent]
CO[协调器代理]
SCH[Scheduler]
CN[Consensus]
MR[ModelRouting]
end
subgraph "任务管理层"
PQ[TaskQueue]
TS[TaskUtils]
end
subgraph "代理层"
AG[Agent]
AP[AgentPool]
TM[Team]
end
subgraph "工具层"
TR[ToolRegistry]
TE[ToolExecutor]
end
subgraph "类型系统"
TY[Types]
end
OM --> CO
OM --> PQ
OM --> SCH
OM --> AP
OM --> TM
OM --> CN
OM --> MR
CO --> AG
PQ --> TS
AP --> AG
AG --> TR
TR --> TE
CN --> TY
MR --> TY
SCH --> TS
```

**图表来源**
- [orchestrator.ts:61-70](file://packages/core/src/orchestrator/orchestrator.ts#L61-L70)
- [scheduler.ts:16-18](file://packages/core/src/orchestrator/scheduler.ts#L16-L18)
- [types.ts:742-745](file://packages/core/src/types.ts#L742-L745)

### 模块耦合分析

- **高内聚低耦合**：每个模块职责明确，接口清晰
- **依赖方向**：协调器层依赖于任务管理和代理执行层
- **循环依赖避免**：通过接口抽象避免循环依赖
- **功能扩展**：新增的共识验证和模型路由功能通过类型系统集成

### 关键接口契约

1. **Agent 接口**：标准化代理行为
2. **Task 接口**：标准化任务表示
3. **Tool 接口**：标准化工具执行
4. **LLMAdapter 接口**：标准化大语言模型适配
5. **Consensus 接口**：标准化验证流程
6. **ModelRouting 接口**：标准化路由策略

**章节来源**
- [orchestrator.ts:61-70](file://packages/core/src/orchestrator/orchestrator.ts#L61-L70)
- [scheduler.ts:16-18](file://packages/core/src/orchestrator/scheduler.ts#L16-L18)

## 性能考虑

### 时间复杂度分析

- **任务解析**：O(n) - n 为任务数量
- **依赖排序**：O(V+E) - V 为任务数，E 为依赖边数
- **调度算法**：O(n log n) - 基于最小堆的调度
- **并发执行**：O(T/P) - T 为总任务时间，P 为并发度
- **共识验证**：O(J×R) - J 为验证者数量，R 为最大轮次
- **模型路由**：O(R) - R 为规则数量（通常很小）

### 内存使用优化

- **增量处理**：任务完成后立即释放内存
- **流式处理**：支持流式生成和处理
- **缓存策略**：合理使用缓存避免重复计算
- **路由缓存**：模型路由规则按需计算和缓存

### 扩展性设计

- **插件架构**：支持自定义调度策略
- **适配器模式**：支持多种 LLM 提供商
- **工具系统**：可扩展的工具执行框架
- **路由扩展**：支持自定义匹配维度和路由配置

## 故障排除指南

### 常见问题诊断

#### 协调器输出解析失败

**症状**：协调器返回非结构化输出

**解决方案**：
1. 检查协调器的 JSON 格式要求
2. 确保输出包含正确的代码围栏
3. 验证 JSON 结构的完整性

#### 任务依赖循环

**症状**：任务无法开始执行

**解决方案**：
1. 使用依赖验证函数检查任务图
2. 识别循环依赖并重新设计任务结构
3. 简化任务依赖关系

#### 并发冲突

**症状**：代理池死锁或资源竞争

**解决方案**：
1. 检查代理池配置的并发度
2. 避免相互委托导致的死锁
3. 实施适当的资源管理策略

#### 共识验证失败

**症状**：共识验证无法达成一致

**解决方案**：
1. 检查验证者的配置和权限
2. 调整 quorum 和 maxRounds 设置
3. 验证 onDissent 处理逻辑
4. 检查预算限制是否过早终止验证

#### 模型路由不生效

**症状**：模型路由规则未按预期工作

**解决方案**：
1. 验证规则的匹配维度设置
2. 检查规则的执行顺序（第一个匹配获胜）
3. 确认任务角色和优先级字段的正确设置
4. 验证叶子节点和依赖关系的判断逻辑

### 调试技巧

#### 日志记录

系统提供详细的进度事件：

- `agent_start`：代理开始执行
- `task_start`：任务开始执行  
- `task_complete`：任务完成
- `error`：错误事件
- `budget_exceeded`：预算超限
- `consensus`：共识验证事件

#### 性能监控

- 监控令牌使用量
- 跟踪任务执行时间
- 分析代理利用率
- 监控共识验证成本
- 追踪模型路由效果

**章节来源**
- [orchestrator.ts:561-818](file://packages/core/src/orchestrator/orchestrator.ts#L561-L818)
- [consensus.test.ts:225-231](file://packages/core/tests/consensus.test.ts#L225-L231)

## 结论

协调器模式为 OpenMultiAgent 框架提供了强大的多代理协作能力。通过临时协调器代理的智能任务分解、完善的依赖管理系统、灵活的调度策略，以及新增的共识验证和模型路由功能，该模式能够高效地处理从简单到复杂的各种任务场景。

### 核心优势

1. **自动化程度高**：减少人工干预，提高执行效率
2. **可扩展性强**：支持任意数量的代理和任务
3. **容错能力强**：完善的错误处理和重试机制
4. **成本可控**：多层次的预算控制和资源管理
5. **质量保证**：集成共识验证确保结果准确性
6. **智能路由**：通过模型路由实现成本优化

### 应用场景

- **复杂项目管理**：大型项目的多阶段执行
- **数据分析流水线**：多步骤的数据处理流程
- **内容创作协作**：多人协作的内容生产
- **软件开发流程**：从设计到部署的完整流程
- **质量保证流程**：需要多角度验证的重要决策
- **成本敏感应用**：需要精确成本控制的场景

### 发展方向

随着人工智能技术的发展，协调器模式将继续演进：

- **更智能的分解算法**：基于上下文理解的任务分解
- **动态调度优化**：实时调整任务分配策略
- **预测性维护**：提前识别和解决潜在问题
- **跨域知识融合**：支持多领域专业知识的协同
- **自适应路由**：根据实时成本和性能动态调整模型选择
- **增强的验证机制**：支持更复杂的验证场景和模式

协调器模式不仅是一个技术实现，更是多智能体协作的哲学体现——通过合理的分工和协调，实现比单一智能体更强的集体智慧。新增的共识验证和模型路由功能进一步强化了这一理念，为构建更智能、更可靠的多代理系统奠定了坚实基础。