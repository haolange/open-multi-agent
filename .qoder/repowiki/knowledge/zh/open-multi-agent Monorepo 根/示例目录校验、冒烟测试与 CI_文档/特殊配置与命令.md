CI 中通过 `npm run test:observability-examples`、`npm run test:eval-examples`、`npm run bench:observability:ci`、`npm run test:scaffold`、`npm run test:observability-pack` 触发本模块下的脚本；本地可通过 `node scripts/validate-example-catalog.mjs` 校验示例目录，或通过 `node scripts/observability-benchmark-ci.mjs` 设置 `OMA_BENCH_ITERATIONS`/`OMA_BENCH_ROUNDS` 环境变量运行基准门控。