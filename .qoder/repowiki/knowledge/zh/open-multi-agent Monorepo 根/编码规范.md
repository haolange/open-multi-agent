- 子包通过 npm workspaces 组织在 `packages/*` 下，并以 `@open-multi-agent/*` 命名空间发布，根仅做编排不承载业务代码。
- 跨包通用任务（构建、测试、lint、示例校验、冒烟测试）集中在根 `package.json` scripts，子包只暴露自身命令供根转发。
- 示例与可观测性基准通过根 `scripts/` 中的 Node 脚本集中验证，而非分散在各子包中重复实现。