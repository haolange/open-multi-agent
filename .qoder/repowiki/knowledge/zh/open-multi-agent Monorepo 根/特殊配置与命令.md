在仓库根执行 `npm run build`、`npm test`、`npm run lint` 即可递归构建/测试/检查所有 packages；`npm run test:example-catalog` 单独校验示例目录清单；`npm run bench:observability:ci`、`npm run test:eval-examples`、`npm run test:observability-examples`、`npm run test:observability-pack`、`npm run test:scaffold` 为根级专项冒烟入口。